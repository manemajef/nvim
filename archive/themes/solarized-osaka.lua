return {
  "craftzdog/solarized-osaka.nvim",
  branch = "osaka",
  lazy = true, -- Load but don't apply
  -- priority = 1000,
  opts = {
    transparent = true,
    styles = {
      sidebars = "transparent",
      floats = "transparent",
    },
  },
}
